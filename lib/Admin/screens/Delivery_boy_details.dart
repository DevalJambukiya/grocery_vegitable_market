import 'package:flutter/material.dart';

class DeliveryBoyDetailsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Delivery Boy Details'),
        backgroundColor: Colors.green,
      ),
      body: Center(child: Text('This is the Delivery Boy Details page')),
    );
  }
}
